# 验证记录

日期：2026-09-22。版本：0.2.0。

## 环境

- macOS，本机 VS Code 1.138.0。
- 官方 Codex 扩展：26.5908.31748，darwin-arm64。
- 启动运行时：VS Code 内置 Node v24.18.1。

## 已通过

- TypeScript 类型检查。
- 6 项必要测试：TOML 数据还原、损坏配置与错误脱敏、官方扩展二进制升级定位、真实子进程标准流与退出信号、两个独立宿主并行隔离，以及扩展命令/工作区持久化/重载/恢复流程。
- 启动时序：先启动 bridge，再注册所属宿主的配置服务，后端正确等待并取得本窗口配置。查找不存在的宿主会明确失败。
- 全局设置边界：首次接管写入统一入口；切换和恢复默认不重写全局设置；停用所有窗口需要单独确认。
- 工作区 A 与 B 保存不同选择，重新激活后恢复各自配置；A 恢复默认不修改 B 的持久化状态。
- `node scripts/smoke.mjs`：先以单个默认后端初始化临时状态库，再通过 VS Code 内置运行时启动两个模拟扩展宿主和真实 Codex 后端，共用一个启动入口，同时完成 `initialize` 与 `config/read`。A 读取 `window_a / smoke-a / high`，B 读取 `window_b / smoke-b / low`。
- 停止 A 并以默认选择重新启动后，A 读取临时 `config.toml` 中的模型和推理强度；未配置 provider 的原始字段返回 `null`。持续运行的 B 再次读取后仍为自己的配置。
- 初始化临时状态库后，上述真实后端并行与恢复默认检查连续 5 轮通过。

真实后端检查使用自动生成的临时配置和占位 provider 地址，不读取个人 profile 或凭据，未创建模型对话、调用模型或改写当前 VS Code 用户设置。测试结束后停止测试宿主及后端，移除临时启动文件。

## 验收边界

扩展命令测试使用 VS Code API 替身；并行验证使用真实独立宿主模拟进程与 Codex 二进制，没有启动两个真实 VS Code 窗口，也没有通过桌面自动化点击状态栏、菜单或重载窗口。VSIX 安装后的原生 UI 与完整扩展宿主启动仍需在用户的 VS Code 中体验确认。Linux、Windows 和远程环境均未实机验证；当前仅提供本机 macOS / Linux 支持。

当前实现依赖官方开发用途设置 `chatgpt.cliExecutable`。CLI 参数覆盖和原生 profile 的层级不同，详见 README。

已只读核对本机官方扩展 `26.5908.31748`：启动路径为 application 作用域；通过当前扩展宿主直接启动子进程，未传项目 cwd；激活函数启动后端后继续注册界面，不等待后端初始化完成。因此使用父进程寻址并等待切换器激活。此结论仅针对已核对版本；分离扩展宿主的 affinity 设置不支持。

## 已发现的上游限制

复核时复现：两个真实后端同时首次使用同一个空配置目录，偶发退出并报告 `failed to initialize sqlite state runtime`。启动器已取得正确窗口配置，失败发生在官方二进制初始化共享状态库阶段。验证脚本因此先用单个后端建立状态库，再启动并行宿主。此改动不修复官方后端的冷启动竞争；用户指定全新配置目录时，应先在单个窗口启动成功。
